library(readxl)

datos_crudos <- read_excel("Viajes origen destino optativa Exactas.xlsx"

head(mi_data)      # Muestra las primeras 6 filas
str(mi_data)       # Muestra la estructura de los datos
summary(mi_data)   # Estadísticas básicas
)
